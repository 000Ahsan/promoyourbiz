{
  "name": "promoyourbiz-eleventy",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "css": "postcss tailwind.css -o public/assets/css/tailwind.css",
    "start": "npm run css && eleventy --serve",
    "build": "npm run css && eleventy",
    "watch": "npm run css && eleventy --watch"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "@tailwindcss/postcss": "^4.2.4",
    "postcss": "^8.5.12",
    "postcss-cli": "^11.0.1",
    "tailwindcss": "^4.2.4"
  }
}
